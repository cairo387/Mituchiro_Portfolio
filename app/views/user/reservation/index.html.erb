<div class="breadcrumb-navigation">
  <i class="fa fa-angle-right">&nbsp;</i>
  <span>HOME<span>
  <i class="fa fa-angle-right">&nbsp;</i>
  <span>CHIROPRACTORS<span>
  <i class="fa fa-angle-right">&nbsp;</i>
  <span>SHOW<span>
  <i class="fa fa-angle-right">&nbsp;</i>
  <span>CONFIRM<span>

<div class="PageHead_practator">
  <div class="PageHead_practator-title">
    <h2>CONFIRM</h2>
  </div>
  <div class="PageHead_practator-text">
    <p>
      予約履歴を確認できるページです。<br>
      予約の入れた日時を確認しておきましょう。
    </p>
  </div>
  <div class="chiropractor-display">
    <div class="col-md-10 mx-auto">
      <div class="reservation_table">
        <table class="table table-hover">
          <thead class="table-active">
            <tr>
              <th>予約開始時間</th>
              <th>予約者氏名</th>
              <th>整体師名</th>
              <th>施術内容</th>
            </tr>
          </thead>
          <tbody>
            <% @reservations.each do |reservation| %>
              <tr>
                <td><%= reservation.display_start_date %></td>
                <td><%= reservation.name %></td>
                <td><%= reservation.chiropractor.nickname %></td>
                <td><%= reservation.chiropractor.treatment.name %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <div class="reservation_paginate">
      <%= paginate @reservations %>
    </div>
  </div>