<div class="breadcrumb-navigation">
  <i class="fa fa-angle-right">&nbsp;</i>
  <span>HOME<span>
  <i class="fa fa-angle-right">&nbsp;</i>
  <span>ARTICELS<span>

<div class="PageHead-article">
  <div class="PageHead-article_title">
    <h2>ARTICLES</h2>
  </div>
  <div class="PageHead-article_text">
    <p>
      投稿された記事の一覧ページです。<br>
      リンクをクリックすることで記事の詳細をご確認いただけます。
    </p>
  </div>

<div class="article-index-display">
  <% @articles.each do |article| %>
    <div class="article-box">
      <%= link_to(article_path(article), class: "article_links") do %>
        <h6 class="article-box_date"><%= article.created_at.strftime('%Y/%m/%d') %></h6>
        <h4 class="article-box_title"><%= article.title.truncate(18) %><h4>
        <%= attachment_image_tag(article, :image, format: 'jpeg', fallback: "no-image.png" ,class: "article-box_image") %>
        <h6 class="article-box_text"><%= article.body.truncate(20) %></h6>
      <% end %>
    </div>
  <% end %>
</div>